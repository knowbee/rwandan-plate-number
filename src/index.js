const {
  isValid
} = require("./isValid")
const {
  isSpecial
} = require("./isSpecial")

const {
  isMoto
} = require("./isMoto")

const {
  isForeign
} = require("./isForeign")

module.exports = {
  isValid,
  isMoto,
  isForeign,
  isSpecial
}