const {
  isValid
} = require("./isValid")
const {
  isSpecial
} = require("./isSpecial")

const {
  isMoto
} = require("./isMoto")

const {
  isForeign
} = require("./isForeign")

const {
  isDiplomatic
} = require("./isDiplomatic")


module.exports = {
  isValid,
  isMoto,
  isForeign,
  isSpecial,
  isDiplomatic
}